spring:
    config:
        activate:
            on-profile: dev
name: dev-YAML
environment: development
server:
  port: 8080
kafka:
  servers:
  awardTopic:
  sslLocation:

---
spring:
    config:
        activate:
            on-profile: test
name: test-YAML
environment: testing
kafka:
  servers:
  awardTopic:
  sslLocation:

---
spring:
    config:
        activate:
            on-profile: prod
name: prod-YAML
environment: production
kafka:
  servers:
  awardTopic:
  sslLocation: